# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

# You can setup an CI pipeline with Github actions and Firebase Hosting by enabling it via the command line. You can setup it all manually, of course.

# name: Deploy to Firebase Hosting on merge
# 'on':
#   push:
#     branches:
#       - main
# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - run: npm ci && npm run build
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: '${{ secrets.GITHUB_TOKEN }}'
#           firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ATLAS_PROTOTYPE_SERVER }}'
#           channelId: live
#           projectId: atlas-prototype-server
#         env:
#           FIREBASE_CLI_PREVIEWS: hostingchannels
